#include<stdio.h>
int main() 
{
  int first, second, *p, *q, sum, sub;
  scanf("%d%d", &first, &second);
  p = &first; q = &second;
  sum = *p + *q;
  printf("%d\n", sum);
  sub = *p - *q;
  printf("%d\n",sub);
  return 0; 
}
